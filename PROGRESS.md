# Eagle — اللي تم واللي باقي

آخر تحديث: **22/09/2026** · المرجع الكامل: `SPEC.md` في جذر المشروع

---

## نظرة سريعة

| المرحلة | الحالة |
|---|---|
| 1 — شات الموظفين + الفلتر بالدور | ✅ اتكتبت · محتاجة تستات تتشغّل |
| 2 — الجروبات الداخلية | ✅ اتكتبت · محتاجة تستات تتشغّل |
| 3 — التسليمة (60 ثانية) | ⬜ لسه |
| 4 — ربط المسار الكامل | ⬜ لسه |
| 5 — اقتراحات الـAI | ⬜ لسه |
| 6 — التنضيف والأرشفة | ⬜ لسه |
| 7 — اسم العميل في رأس الشات | ⬜ لسه |

⚠ **مفيش حاجة من دول اتعملها commit ولا push ولا deploy.** كلها على الجهاز بس.

---

## ✅ المرحلة 1 — شات الموظفين والفلتر بالدور

### الصفحة بقت بتتغيّر حسب الدور

| الدور | التبويبات |
|---|---|
| المترجم | شاتات الموظفين · الجروبات |
| التيم ليدر | شاتات الموظفين · الجروبات |
| الأوبريشن / الأدمن | العملاء · شاتات الموظفين · الجروبات |

تبويب «العملاء» **مش موجود** عند المترجم — مش موجود ومقفول. ولو كتب
`?type=clients` بنفسه، الصفحة بتفتح على تبويبه الأول من غير رسالة خطأ.

### شات الموظفين

- **الدليل هو القايمة:** كل موظفي الشركة بأسمائهم. اللي كلمته قبل كده فوق
  بآخر رسالة، واللي عمرك ما كلمته تحت بمعاينة فاضية.
- **فتح الصفحة هو اللي بيعمل الغرفة** — مفيش خطوة «ابدأ محادثة».
- السيرش بيدوّر بالاسم بدل كود العميل.
- الصورة في القايمة: العميل أرقام كوده · الجروب أيقونة ناس · الزميل الحرفين
  الأولين من اسمه بلون مختلف.

### المفتاح اللي بيمنع الشاتين

`ChatRoom.pair_key` = `<أصغر id>-<أكبر id>`، و**فريد** في الجدول. لو الاتنين
فتحوا بعض في نفس اللحظة، واحد بس ينجح والتاني بيتوجّه للغرفة اللي اتعملت.
مستحيل يبقى فيه شاتين لنفس الاتنين، مش لأن الكود شاطر — لأن الإندكس مانعها.

### ⚠ قرار محتاج موافقتك

**شات الموظفين مقفول حتى على الأدمن.** في كل غرفة تانية الأدمن بيدخل من غير
عضوية وسيبتها كده. بس الشات 1:1 لأ — شات بين اتنين حد تالت بيقراه مش شات خاص،
والناس لما تعرف ده هترجع تتكلم على واتساب.

فيه تست بيثبّت السلوك ده. **لو عايز الأدمن يشوفه، قول وأنا أغيّره في سطرين.**

### حاجتين اتصلحوا في الطريق

1. **كان هيبقى تسريب:** زرار «ملف العميل» في رأس الشات كان شرطه `show_filter`،
   واللي بقى ظاهر للكل. يعني التيم ليدر في جروب التاسك كان هيلاقي الزرار
   ويروح على صفحة بيانات العميل. بقى شرطه `sees_all_clients`.
2. منع إضافة عضو تالت لشات 1:1 — الداخل الجديد كان هياخد كل اللي اتقال قبله.

---

## ✅ المرحلة 2 — الجروبات الداخلية

### الجروب الداخلي

نوع غرفة جديد `RoomKind.TEAM`: **اسم + أعضاء + مفيش عميل ومفيش تاسك**.
مش بيتعمل تلقائي — حد بيعمله بإيده.

**الاسم الافتراضي:** لما التيم ليدر يعمل جروب ويختار **مترجم واحد بالظبط**،
الاسم بيتملى لوحده `اسم المترجم (اسم التيم ليدر)` — زي ما طلبت. لو اختار
اتنين مترجمين، **مفيش تخمين** — اسم غلط أسوأ من خانة فاضية.

القاعدة مكتوبة في مكانين متطابقين عن قصد: في الجافاسكريبت عشان الخانة تتملى
وأنت بتختار، وفي السيرفر عشان لو سبت الخانة فاضية الاسم يتحسب برضه.

### مين يعمل جروب شغل

التيم ليدر · الأوبريشن · الأدمن. **المترجم لأ** — بيتضاف للجروبات مش بيعملها.
القاعدة في مكان واحد (`User.can_create_team_group`) فسهل توسّعها لو غيّرت رأيك.

### التبويب فيه نوعين دلوقتي

الجروبات الداخلية الجديدة، **وغرف العملاء القديمة** (`TSK-xxxxx`) اللي لسه
شغّالة لحد المرحلة 6. عشان الفرق يبان:

- صف غرفة العميل عليه **شارة «مع العميل»** في القايمة.
- وفوق في الغرفة: غرفة العميل ليها تحذير أصفر «الجروب ده بيوصل العميل»،
  والجروب الداخلي ليه ملاحظة «جروب شغل داخلي — مفيش حاجة هنا بتوصل العميل».
- وفي الكود: `room.reaches_client` — الترحيل والبانر الاتنين بيقروا منها.

### مين بيشوف إيه في قايمة الجروبات

| النوع | القاعدة |
|---|---|
| غرفة عميل | الأعضاء، **والأدمن بيشوف الكل** (زي ما كانت) |
| جروب شغل | الأعضاء بس، **الأدمن كمان** |

الفرق مقصود: غرفة العميل دي محادثة الشركة مع عميل فالأدمن بيشرف عليها، وجروب
الشغل جروب — يا إنت فيه يا لأ. والأدمن لسه يقدر يفتح جروب باللينك لو احتاج
(`can_access` بتسمح) — بس مش هيلاقي مية جروب في قايمته.

---

## ⬜ اللي باقي

### المرحلة 3 — التسليمة

كارت في الشات فيه ملفات التاسك وزرار **استلمت** و**رفض** وعدّاد **60 ثانية**.
معاينة الملف قبل القرار · الرفض بسبب إجباري · انتهاء الوقت = رفض تلقائي يرجع
للي بعت.

⚠ **أخطر نقطة في المشروع كله:** الرفض التلقائي **لازم** يتحسب على السيرفر.
العدّاد في المتصفح عرض بس — لو محدش فاتح الصفحة، مفيش جافاسكريبت تشتغل. فالحالة
بتتحسب من حتّتين: قراءة كسولة (أي فتح للتسليمة بعد الميعاد يعتبرها منتهية)
**و** `manage.py sweep` اللي بيكتب الحالة ويبعت التنبيه.

**لو `sweep` مش متجدولة على PythonAnywhere، التاسك هتقف من غير ما حد يعرف.**

### المرحلة 4 — ربط المسار

أوبريشن → ليدر → مترجم → ليدر → أوبريشن → العميل.

⚠ **المرتبات مبنية على ملفات التاسك.** `payroll` و`wordcount` بيقروا من
`Task.source_files` اللي الجروب كان بيكتبها. التسليمة الجديدة **لازم** تكتبها
بنفس الشكل وإلا حساب المترجمين بيقع بالسكوت.

### المرحلة 5 — اقتراحات الـAI

نافذة عند الليدر لما الملف المترجَم يوصله. **اقتراحات بس، مفيش تطبيق تلقائي.**

### المرحلة 6 — التنضيف

أرشفة غرف العميل وجروبات التاسك القديمة · صفحة التاسك تبقى عرض بس.

⚠ **الغرف القديمة فيها كلام حقيقي مع عملاء حقيقيين.** الخطة **أرشفة مش مسح**.
المسح قرارك إنت لوحدك.

### المرحلة 7 — رأس الشات

للأدمن: `اسم العميل (CL-0002)` والكود بخط أصغر. **تغيير عرض بس** —
`can_see_client_identity` ماتتوسّعش.

---

## ❓ أسئلة لسه مفتوحة

1. رجلين الرجوع (مترجم→ليدر، ليدر→أوبريشن) — عدّاد 60 ثانية كمان ولا بعتة
   عادية؟ (مكتوبة في المواصفة بعتة عادية مبدئيًا)
2. المترجم رفض والليدر بعتها لواحد تاني — فيه حد لعدد الرفضات قبل ما ترجع
   للأوبريشن؟
3. المترجم يقدر يبدأ تسليمة بنفسه ولا بيستقبل ويرجّع بس؟
4. ملفات الجروبات القديمة — تتنقل للتاسك ولا تفضل مكانها؟
5. شات الموظفين — يفضل مقفول على الأدمن ولا يفتح؟

---

## اللي لازم تشغّله دلوقتي

```
python manage.py migrate
python manage.py test dashboard.tests.StaffChatTests dashboard.tests.TeamGroupTests --settings=Core.settings_test
python .claude\verify.py
```

**24 تست جديد** (10 للمرحلة 1 + 14 للمرحلة 2) لسه مااتشغلوش ولا مرة.
اللي اتعمل لحد دلوقتي فحص ساكن بس: `py_compile` · `node --check` ·
توازن تاجات التمبليت · أسماء الأيقونات · أسماء الـURL · الكونفنشنز — كلهم نضاف.

### ميجريشن

`0018_staff_chats` (عمود `pair_key`) · `0019_team_groups` (choices بس، مفيش
تغيير في الداتابيز).

### ملفات اتغيّرت

`dashboard/`: `models.py` · `services.py` · `views.py` · `urls.py` · `api.py` ·
`tests.py` · `migrations/0018_staff_chats.py` · `migrations/0019_team_groups.py`

`templates/ops/chats.html` · `static/js/chat.js` · `static/css/chat.css`

النشر هيحتاج `collectstatic` (الـCSS والـJS اتغيّروا).
