# Eagle — اللي تم واللي باقي

آخر تحديث: **22/09/2026** · المواصفة الأصلية: `SPEC.md`

---

## الحالة

| المرحلة | الحالة |
|---|---|
| 1 — شات الموظفين + الفلتر بالدور | ✅ اتكتبت |
| 2 — الجروبات الداخلية | ✅ اتكتبت |
| 3 — التسليمة (60 ثانية) | ✅ اتكتبت — **بتوسيع `Assignment` مش بموديل جديد** |
| 4 — ربط المسار | ✅ اتكتبت |
| 5 — اقتراحات الـAI | ✅ اتكتبت |
| 6 — التنضيف والأرشفة | ✅ اتكتبت — **جزئيًا، اقرا تحت** |
| 7 — اسم العميل في رأس الشات | ✅ اتكتبت |

🔴 **مفيش حاجة اتعملها commit ولا push ولا deploy.** كلها على الجهاز بس.
🔴 **الـ55 تست الجديدة لسه مااتشغلوش ولا مرة.**

---

## اللي لازم تشغّله

```
python manage.py migrate
python manage.py test dashboard --settings=Core.settings_test
python .claude\verify.py
```

بعد ما يعدّي، لو عايز تشيل شاتات التجربة القديمة من القوايم:

```
python manage.py archive_client_rooms --dry-run
python manage.py archive_client_rooms
```

---

## ✅ 1 — شات الموظفين والفلتر بالدور

| الدور | التبويبات |
|---|---|
| المترجم | شاتات الموظفين · الجروبات |
| التيم ليدر | شاتات الموظفين · الجروبات |
| الأوبريشن / الأدمن | العملاء · شاتات الموظفين · الجروبات |

- **الدليل هو القايمة:** كل الموظفين بأسمائهم، اللي كلمته آخر مرة فوق واللي
  عمرك ما كلمته تحت. فتح الصف = فتح الشات.
- `pair_key` فريد: مستحيل يبقى فيه شاتين لنفس الاتنين، لأن الإندكس مانعها.
- ⚠ **شات الموظفين مقفول حتى على الأدمن** — قرار مستني تأكيدك.

## ✅ 2 — الجروبات الداخلية

- `RoomKind.TEAM`: اسم + أعضاء، **مفيش عميل ومفيش تاسك ومفيش ترحيل**.
- الاسم الافتراضي `اسم المترجم (اسم التيم ليدر)` لما الليدر يختار مترجم واحد.
  اتنين = مفيش تخمين. القاعدة في الجافاسكريبت **وفي السيرفر**.
- مين يعمل: الليدر · الأوبريشن · الأدمن. المترجم لأ.
- `room.reaches_client` هي اللي بتفرّق بين الجروب اللي بيوصل العميل واللي لأ —
  الترحيل والبانر والشارة التلاتة بيقروا منها.

## ✅ 3 و 4 — التسليمة والمسار

### 🔴 خالفت المواصفة هنا عن قصد

`SPEC.md` طلب موديل `Handoff` جديد. **لقيت اللي فيه مبني بالفعل** في
`Assignment`: العدّاد (`expires_at` + `response_window_seconds`)، القبول،
الرفض، الرفض التلقائي (`sweep_expired_assignments`)، خصم التقييم، والمودال
بالعدّاد والصوت. والـ`sweep` بيشتغل **من نبض المتصفح كمان** مش من الجدولة بس.

بناء `Handoff` جنبه كان هيبقى **مصدرين للحقيقة** لسؤال واحد: «مين ماسك التاسك
وهل وافق؟». فوسّعت الموجود بالتلاتة اللي كانوا ناقصين فعلًا:

| اللي اتضاف | التفصيل |
|---|---|
| `Assignment.room` | ملفات العميل بتنزل في **الشات 1:1** بين اللي بعت واللي استلم، بنفس الآلية اللي بتعدّي الملف من غير اسم العميل ولا رقمه |
| `Assignment.opened_at` | «شوف الملفات الأول» — بيوديه على الشات والعدّاد شغّال. **فتح الملف مش قبول** |
| `Assignment.reason` | الرفض بسبب إجباري. السيرفر بيرفض من غيره، مش الواجهة بس. والسبب بيتكتب في نفس الشات. ولما الوقت يخلص بيتكتب لوحده |

**رجلين الرجوع:** المترجم يدوس «خلصت» → ملاحظة في شات الليدر. الليدر يدوس
«تمت المراجعة» → ملاحظة في شات اللي عمل التاسك. **من غير عدّاد** — إنت
ماذكرتش 60 ثانية عليهم.

## ✅ 5 — اقتراحات الـAI

لما المترجم يخلص، الفحص التلقائي بيشتغل (ده كان موجود). الجديد: **الليدر بيلاقي
الاقتراحات في شات المترجم نفسه**، لوحة مطوية فيها الملاحظات وكود التاسك.

⚠ **مكتوبة كاقتراحات مش تصحيحات**، و**محدش بيشوفها غير الليدر** — مابتتكتبش في
الشات المشترك. نقد آلي لشغل حد، بيوصله في الشات اللي بيقراه كل يوم، ده مش
مراجعة — ده تصحيح علني. فيه تستين بيثبّتوا ده.

## ✅ 6 — التنضيف

- `ChatRoom.is_archived`: الغرفة بتختفي من القوايم و**بتفضل مفتوحة بلينكها**.
- أمر `archive_client_rooms` (وعنده `--dry-run` و`--undo`) بيأرشف غرف العميل
  وغرف التاسك القديمة. **الجروبات الداخلية وشاتات الموظفين مابيتلمسوش.**
- **مفيش مسح.** إنت قلت إن الشاتات القديمة تجربة، فأرشفتها كفاية — الأرشفة
  بترجع، المسح لأ.

### ⚠ الجزء اللي مانفذتوش من المرحلة 6

المواصفة كانت بتقول كمان «صفحة التاسك تبقى عرض بس». **مانفذتوش عن قصد.**

السبب: الجملة دي كانت مبنية على إن كل الأزرار هتنتقل للشات. وأنا ماعملتش كده —
خليت `Assignment` والمودال، والأزرار (خلصت · تمت المراجعة · استلمت التاسك ·
التسليم) لسه في صفحة التاسك وشغّالة. **لو خليتها عرض بس دلوقتي، المسار هيقف** —
مش هيبقى فيه مكان تاني تدوس فيه.

لو عايز الأزرار تنتقل للشات فعلًا، دي شغلانة لوحدها ولازم تتقال.

## ✅ 7 — رأس الشات

للأدمن: **`ACME Legal (CL-0002)`** — الاسم الأول والكود جنبه بخط أصغر وباهت.

⚠ **تغيير عرض بس.** `can_see_client_identity` **ماتوسعتش** — الأوبريشن والليدر
والمترجم لسه شايفين الكود لوحده، وفيه تست بيتأكد.

---

## الميجريشن

| | |
|---|---|
| `0018_staff_chats` | عمود `pair_key` |
| `0019_team_groups` | choices بس |
| `0020_handoff_in_chat` | `reason` · `opened_at` · `room` على `Assignment` |
| `0021_archive_rooms` | عمود `is_archived` |

## التستات الجديدة

| الكلاس | العدد |
|---|---|
| `StaffChatTests` | 10 |
| `TeamGroupTests` | 14 |
| `HandoffInChatTests` | 15 |
| `SuggestionsArchiveAndHeaderTests` | 13 |
| `ActionsFollowTheFilesTests` (من قبل) | 7 |

اللي اتعمل لحد دلوقتي **فحص ساكن بس** — `py_compile` · `node --check` · توازن
تاجات التمبليت · أسماء الأيقونات · أسماء الـURL · الكونفنشنز. كلهم نضاف.

---

## ملفات اتغيّرت

`dashboard/`: `models.py` · `services.py` · `views.py` · `urls.py` · `api.py` ·
`tests.py` · أربع ميجريشن · `management/commands/archive_client_rooms.py`

`templates/`: `ops/chats.html` · `partials/assign_modal.html`

`static/`: `js/chat.js` · `js/app.js` · `css/chat.css`

**النشر محتاج `collectstatic`** — التلات ملفات static اتغيّروا.

---

## ❓ مستني ردك على تلاتة

1. **شات الموظفين مقفول على الأدمن** — يفضل كده ولا يفتح؟
2. **رجلين الرجوع من غير عدّاد** — تمام ولا عايزهم بـ60 ثانية برضه؟
3. **صفحة التاسك لسه فيها الأزرار** — تفضل كده ولا ننقلهم للشات (شغلانة لوحدها)؟
